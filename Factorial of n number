#include<stdio.h>
int main ()
{
	int i,fact=1,n;
	printf("enter number");
	scanf("%d",&n);
	if (n<0)
	printf("invalid input");
	else
	{
		
	for(i=1;i<=n;i++)
	{
		fact*=i;
	}
 
	printf("factorial of %d=%d",n,fact);
	}
	return 0;
}
